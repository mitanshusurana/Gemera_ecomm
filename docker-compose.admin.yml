services:
  admin:
    build:
      context: .
      dockerfile: admin.Dockerfile
    container_name: jewelry-admin
    restart: always
    environment:
      # Change API_URL to point to the remote backend IP/domain
      API_URL: ${API_URL:-http://localhost:8080/api/v1}
    ports:
      - "4300:80"