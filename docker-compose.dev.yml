# LOCAL DEV — All services running locally, DB on Oracle VM

# LOCAL DEV — All services running locally, DB on Oracle VM
# Usage: docker compose -f docker-compose.dev.yml up --build

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gemera-backend-dev
    restart: unless-stopped
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://129.159.18.63:5432/jewelry_db
      SPRING_DATASOURCE_USERNAME: caratloop
      SPRING_DATASOURCE_PASSWORD: naitiK@23
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: org.hibernate.dialect.PostgreSQLDialect
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      ADMIN_EMAIL: admin@caratloop.com
      ADMIN_PASSWORD: naitik@23
      JWT_SECRET: b8c3a9f2e4d7601f5c82a3b9e6d4f10c7a5b2e8d0f9c6a3b7e4d2f8c0a5b9e3d6
      R2_ACCESS_KEY: b6732f19ad8e84b89984e96c78d6a025
      R2_SECRET_KEY: ea7b90f35f348cb25d590bd288eb4c3099519be8daaa17f65bddd57d7f128696
      R2_ENDPOINT: https://3145274f44bbf3178e1f2469ff4fdb07.r2.cloudflarestorage.com
      R2_BUCKET_NAME: suranagemsassets
      R2_PUBLIC_URL: https://pub-edd8f524b4784df1b5961ce0d431f767.r2.dev
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USERNAME: mitanshusurana@gmail.com
      MAIL_PASSWORD: PASTE_16_CHAR_APP_PASSWORD_HERE
      MAIL_FROM: mitanshusurana@gmail.com
      GOLDAPI_KEY: demo
    ports:
      - "8080:8080"

  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    container_name: gemera-frontend-dev
    restart: unless-stopped
    environment:
      API_URL: http://localhost:8080/api/v1
      SSR_API_URL: http://backend:8080/api/v1
      RAZORPAY_KEY: rzp_test_S5goGHXLEuP6hP
      WHATSAPP_NUMBER: 917976091951
      NODE_ENV: production
      PORT: 4000
      HOST: 0.0.0.0
      NG_ALLOWED_HOSTS: localhost,127.0.0.1
    ports:
      - "4200:4000"
    depends_on:
      - backend

  admin:
    build:
      context: .
      dockerfile: admin.Dockerfile
    container_name: gemera-admin-dev
    restart: unless-stopped
    environment:
      API_URL: http://localhost:8080/api/v1
    ports:
      - "4300:80"
    depends_on:
      - backend


