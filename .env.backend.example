# ==========================================
# CORE VM: database, API and admin (docker-compose.backend.yml)
# Copy to .env.backend. Every variable without a default is required: the API refuses to start otherwise.
# ==========================================

# --- PostgreSQL (container) ---
POSTGRES_DB=jewelry_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_db_password

# --- API: database ---
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/jewelry_db
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=CHANGE_ME_db_password
# Schema is managed by Hibernate (no migration tool). Keep "update"; "validate" only works on an already-created schema.
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# --- API: security ---
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32
ADMIN_EMAIL=admin@caratloop.com
ADMIN_PASSWORD=CHANGE_ME_admin_password
# Browser origins allowed to call the API: storefront and admin, comma separated, scheme included.
CORS_ALLOWED_ORIGINS=https://www.caratloop.com,https://admin.caratloop.com
# Storefront origin used in password-reset emails.
FRONTEND_URL=https://www.caratloop.com

# --- API: email (SMTP) ---
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your_16_char_app_password
MAIL_FROM=your-email@gmail.com

# --- API: payments (Razorpay). Leave empty to disable online payment; gift cards and card checkout then return 503. ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# Secret set on the Razorpay dashboard webhook for https://<api-host>/api/v1/payments/webhook. Empty => webhook answers 401.
RAZORPAY_WEBHOOK_SECRET=

# --- API: ERP integration (optional) ---
# Store invoices and refunds are queued in erp_sync_events and pushed every 5 minutes to
# ${ERP_BASE_URL}/api/v1/integrations/ecommerce/{sales,credit-notes} with header X-Api-Key.
# Leave both empty to run without the ERP: events stay PENDING and nothing is sent.
ERP_BASE_URL=
ERP_API_KEY=

# --- API: WhatsApp notifications (optional; Meta WhatsApp Cloud API) ---
# WHATSAPP_PROVIDER=meta sends approved template messages (order, repair, exchange, appointment, treasure events);
# anything else disables the channel and only e-mail goes out. Template names default to caratloop_<event> and can be
# overridden per event in Admin > Settings > Messaging. The token is never logged.
WHATSAPP_PROVIDER=disabled
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_DEFAULT_LANGUAGE=en

# --- API: SMS notifications (optional) ---
# SMS_PROVIDER=msg91 uses the MSG91 Flow API (SMS_API_KEY = authkey, SMS_SENDER_ID = DLT sender, SMS_TEMPLATE_ID = flow/DLT
# template id whose first variable receives the whole message; SMS_ENDPOINT optional, defaults to the MSG91 flow URL).
# SMS_PROVIDER=http posts JSON {to, message, senderId} to SMS_ENDPOINT with "Authorization: Bearer SMS_API_KEY".
# Customers receive SMS only after switching it on in their account (default off).
SMS_PROVIDER=disabled
SMS_API_KEY=
SMS_SENDER_ID=
SMS_ENDPOINT=
SMS_TEMPLATE_ID=

# --- API: metal prices ---
GOLDAPI_KEY=your_goldapi_key

# --- API: Cloudflare R2 object storage (product and hero images) ---
R2_ACCESS_KEY=your_r2_access_key
R2_SECRET_KEY=your_r2_secret_key
R2_ENDPOINT=https://your-account.r2.cloudflarestorage.com
R2_BUCKET_NAME=jewelry-assets
R2_PUBLIC_URL=https://pub-your-account.r2.dev

# --- Admin SPA (runs in the browser, so these must be PUBLIC URLs) ---
API_URL=https://api.caratloop.com/api/v1
STOREFRONT_URL=https://www.caratloop.com

# --- Image overrides (optional) ---
# BACKEND_IMAGE=ghcr.io/mitanshusurana/gemera_ecomm-backend:latest
# ADMIN_IMAGE=ghcr.io/mitanshusurana/gemera_ecomm-admin:latest
